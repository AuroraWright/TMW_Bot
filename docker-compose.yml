services:
  tmw-bot:
    image: discord-tmw-bot
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    environment:
      - TOKEN
      - AUTHORIZED_USERS
      - DEBUG_USER
      - COMMAND_PREFIX
      - PATH_TO_DB
      - DATABASE_ENCRYPTION_KEY
      - TMDB_API_KEY
    volumes:
      - ./data:/app/data
    tmpfs:
      - /tmp:size=1g,mode=1777
    restart: unless-stopped
